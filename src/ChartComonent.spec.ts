/// <reference path="../typings/browser.d.ts"" />

import {
    ComponentFixture,
    describe,
    expect,
    fakeAsync,
    injectAsync,
    it,
    tick,
    TestComponentBuilder
} from 'angular2/testing';
describe('spec', () => {

    it('should pass', () => {
        expect(42).toEqual(42);
    });
});
